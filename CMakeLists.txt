cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
# v0.9.1 (UI — alt köşede WiFi 4-bar signal indicator + canlı IP/dBm; static "DEMO" kaldırıldı)
set(PROJECT_VER "0.9.1")
project(carcluster)
