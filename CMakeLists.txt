cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
# v0.9.5 (UI rename: "Theme" → "View" — cluster terminolojisinde view daha tutarlı)
set(PROJECT_VER "0.9.5")
project(carcluster)
